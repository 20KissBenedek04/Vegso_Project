<Window x:Class="ZooAdminApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Zoo Admin – Bejelentkezés"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Height="400" Width="640">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Zoo Admin" FontSize="22" FontWeight="Bold" Margin="0,0,0,16" />

        <StackPanel Grid.Row="1">
            <Label Content="Email"/>
            <TextBox x:Name="EmailBox" Height="28" />
            <Label Content="Jelszó" Margin="0,10,0,0"/>
            <PasswordBox x:Name="PasswordBox" Height="28" />
        </StackPanel>

        <Button Grid.Row="2" Content="Bejelentkezés" Height="36" Margin="0,16,0,0" Click="Login_Click"/>
        <TextBlock Grid.Row="3" x:Name="ErrorText" Foreground="Red" FontWeight="Bold" Margin="0,12,0,0" TextWrapping="Wrap"/>
    </Grid>
</Window>